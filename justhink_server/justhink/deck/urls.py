import django.urls

import deck.views

app_name = "deck"


 

urlpatterns = [
    # django.urls.path(
    #     "",
    #     deck.views.DeckAPIList.as_view(),
    #     name="deck-list",
    # ),
    # django.urls.path(
    #     "<int:pk>/",
    #     deck.views.DeckAPIDetail.as_view(),
    #     name="deck-detail",
    # ),
    # django.urls.path(
    #     "",
    #     deck.views.DeckViewSet.as_view({"get": "list"}),
    #     name="deck-list",
    # ),
    # django.urls.path(
    #     "<int:pk>/",
    #     deck.views.DeckViewSet.as_view({"put": "update"}),
    #     name="deck-detail",
    # ),
]
